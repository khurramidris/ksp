# Karachi Sitar Project

A scroll-driven sitar-learning experience for Karachi, built as an illuminated cream Mughal manuscript.

## Run locally

```bash
npm install
npm run dev
```

The deployable static site lives in `dist/`. `npm run build` validates the HTML, assets, anchors and motion fallback.
